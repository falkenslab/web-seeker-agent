"""Inicialización del paquete `agent`.

Carga variables de entorno desde un archivo ``.env`` para configurar credenciales
y parámetros necesarios por los componentes del agente.
"""

from dotenv import load_dotenv

load_dotenv()
